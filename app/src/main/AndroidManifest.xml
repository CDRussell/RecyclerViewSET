<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cdrussell.recyclerviewtransition">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".CustomApplication"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity_to_activity_list_to_detail.ListToDetail1RecyclerViewActivity" />
        <activity android:name=".activity_to_activity_list_to_detail.ListToDetail2RecyclerViewActivity" />

        <activity android:name=".activity_to_activity_detail_to_list.DetailToList1RecyclerViewActivity" />
        <activity android:name=".activity_to_activity_detail_to_list.DetailToList2RecyclerViewActivity" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>